import React from 'react';
import ReactDOM from 'react-dom';

const elem = (
  <div>
    <h2>Clean project</h2>
    <input type="text"/>
    <button/>
  </div>
);

ReactDOM.render(
    elem,
  document.getElementById('root')
);